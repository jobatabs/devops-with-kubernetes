apiVersion: v1
kind: Service
metadata:
  name: pingpong-backend-svc
  namespace: exercises
spec:
  type: ClusterIP
  selector:
    app: pingpong
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
    